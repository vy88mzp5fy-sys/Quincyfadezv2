# Public mobile configuration only. Never put a Stripe secret key here.
EXPO_PUBLIC_API_URL=https://your-backend.example.com

# Optional: the app can also fetch this publishable key from /api/payments/config.
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
